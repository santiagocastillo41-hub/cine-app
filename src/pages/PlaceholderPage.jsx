export default function PlaceholderPage({ title }) {
  return (
    <div className="flex flex-1 items-center justify-center px-4 text-center">
      <p className="type-body-sm text-text-tertiary">{title} — todavía no existe esta pantalla.</p>
    </div>
  )
}
