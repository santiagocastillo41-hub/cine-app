# Supabase — cliente (expuestas al navegador, prefijo VITE_ obligatorio)
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=

# Supabase — sólo scripts locales (seed, migraciones). Nunca en el cliente.
SUPABASE_SECRET_KEY=

# TMDB — sólo scripts locales (sync de películas). Nunca en el cliente.
TMDB_READ_TOKEN=
